{{- if .Values.cronjob.enabled -}}
apiVersion: batch/v1beta1
kind: CronJob
metadata:
  name: trigger-events-update
  labels:
    {{- include "app.labels" . | nindent 4 }}
spec:
  schedule: "*/1 * * * *"
  successfulJobsHistoryLimit: 3
  failedJobsHistoryLimit: 3
  concurrencyPolicy: Forbid
  jobTemplate:
    metadata:
      labels:
        cron-job: trigger-events-update
    spec:
      backoffLimit: 3
      activeDeadlineSeconds: 30
      template:
        metadata:
          labels:
            cron-job: trigger-events-update
        spec:
          containers:
          - name: trigger-events-update
            image: radial/busyboxplus:curl
            imagePullPolicy: IfNotPresent
            args:
            - /bin/sh
            - -c
            - http_status=$( curl -v -H "Content-Type: application/json" -w '%{http_code}' http://offender-assessments-events.offender-assessments-events-dev.svc.cluster.local/events )
            - echo ${http_status}
          restartPolicy: OnFailure
{{- end }}